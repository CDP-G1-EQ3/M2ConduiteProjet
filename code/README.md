# CDP Project

Start the project : 

`docker-compose up -d`

After modification : 

`docker-compose up --detach --build`


Start from scratch :

`sudo docker-compose up --build --force-recreate --renew-anon-volumes`