| ID | US ID | Task | Durée | Dépendance | DoD | État | Assigné |
|:--:|:-----:|:-----|:-----:|:----------:|-----|:----:|:-------:|
|T01|01| Créer et implémenter le template `register.ejs` contenant le formulaire d'ajout d'un nouvel utilisateur. Le formulaire devra contenir les champs: adresse e-mail, pseudonyme, mot de passe et confirmation du mot de passe. Un bouton "S'inscrire" devra permettre l'inscription de l'utilisateur et un bouton "Se connecter" devra rediriger vers la page de connexion (login).  La page devra contenir un élément html (div ou alert bootstrap) pour afficher les éventuelles erreurs. | 1 |  |  | TODO ||
|T02|01| Créer et implémenter le fichier `RegisterController.js` pour l'enregistrement d'un nouvel utilisateur. L'implémentation devra permettre de gérer une requête GET pour l'affichage du template `register.ejs` et une requête POST pour l'enregistrement du nouvel utilisateur. Il faudra vérifier que les deux mots de passe reçus sont identiques et que le pseudonyme entré n'est pas utilisé par un autre utilisateur. Si les informations reçues depuis le formulaire sont valides alors rediriger l'utilisateur vers la page de connexion (login) sinon l'utilisateur reste sur la même page affichant les erreurs produites. | 2 |  | | TODO ||
| T03 | 02 | Créer et implémenter le template `login.ejs` contenant le formulaire de connexion pour un utilisateur. Le formulaire devra permettre la saisie d'une adresse e-mail et d'un mot de passe. Un bouton "Se connecter" devra permettre la connexion de l'utilisateur et un lien "Créer un compte" devra rediriger l'utilisateur vers la page d'inscription (register). La page devra contenir un élément html (div ou alert bootstrap) pour afficher les éventuelles erreurs. | 1 | | | TODO ||
| T04 | 02 | Créer et implémenter le contrôleur `LoginController.js` qui devra proposer une route GET pour afficher la page de connexion (`login.ejs`) et une route POST pour l'authentification. Si l'authentification a réussi alors rediriger l'utilisateur sur la page contenant la liste de ses projets (home). Sinon le rediriger vers la page de connexion en affichant les erreurs survenues. | 2| | | TODO ||
| T05 | 03 | Créer et implémenter le template `createProject.ejs` contenant un formulaire pour la création d'un projet. Le formulaire devra contenir un champ pour le titre du projet (obligatoire) et un autre pour la description du projet (optionnel). Un bouton "Créer" devra valider le formulaire. | 1 | | | TODO ||
| T06 | 03 | Créer et implémenter le contrôleur `CreateProjectController.js` qui devra proposer une route GET pour afficher la page de création de projet (createProject) et une route POST pour ajouter un projet dans la base de données. Il faudra s'assurer que le nom du projet entré n'est pas déjà enregistré dans la base données. Si l'ajout du projet a réussi alors rediriger l'utilisateur sur la page contenant la liste de ses projets (home). Sinon le rediriger sur la page de création de projet (createProject) avec un message affichant l'erreur survenue. | 2 | | | TODO ||
| T07 | 05 | Créer et implémenter le template `home.ejs` qui va représenter la page d'accueil de l'utilisateur. Cette page devra proposer un bouton "Créer projet", un champ de recherche et une liste des projets auxquels l'utilisateur participe. La liste des projets devra être affichée sous forme de tableau ayant les colonnes noms, description et propriétaire. un clic sur le nom d'un projet devra rediriger l'utilisateur vers le backlog du projet (/backlog/projectId). Un clic sur le bouton "créer projet" devra rediriger l'utilisateur vers le template de création de projet (createProject). | 2 | | | TODO |Arnaud|
| T08 | 05 | Créer et implémenter le contrôleur `HomeController.js` proposant une route GET (/home/:userId) pour récupérer la liste des projets de l'utilisateur dont l'id est passé en paramètre dans la requête. L'utilisateur devra être redirigé vers la page home avec une variable contenant la liste des projets récupérer. Si aucun projet n'a été trouvé alors retourner une liste vide. | 1 | | | TODO |Arnaud|
| T09 | 09 | Créer le template `backlog.ejs` et y ajouter un bouton "créer U.S". Un clic sur ce bouton devra afficher une modale (bootstrap) contenant le formulaire d'ajout d'une US avec les champs description, priorité et difficulté. Un bouton "valider" (sur la modale) devra valider le formulaire et un bouton cancel (sur la modale) devra fermer le formulaire. | 2 | | | TODO ||
| T10 | 09 | Créer le contrôleur `backlogController.js` et implémenter une route POST qui permettra d'ajouter une US dans la base de données. Une fois l'ajout effectué rediriger l'utilisateur vers le template backlog. | 1 | | | TODO ||
| T11 | 11 | Ajouter dans le fichier `backlog.ejs` un tableau avec les colonnes description, identifiant, importance et difficulté pour afficher les U.S d'un projet de l'utilisateur. | 2 | | | TODO ||
| T12 | 11 | Ajouter dans le contrôleur BacklogController.js une route GET (/backlog/:projectId) pour récupérer les U.S d'un projet. Une fois les U.S récupérées dans la base de données, rediriger l'utilisateur vers le template backlog avec une variable contenant la liste des U.S récupérées. Si aucune U.S n'a été trouvée alors renvoyer une liste vide. | 2 | | | TODO ||
| T13 | 10 | Dans `backlog.ejs` faire en sorte qu'un clic sur une U.S du backlog fasse apparaître (à droite du backlog) un formulaire prérempli avec les informations de l'U.S sur laquelle l'utilisateur a cliqué. Les informations à mettre dans le formulaire sont la description de l'US, son importance (liste déroulante), et sa difficulté. Un bouton "modifier" devra fair appel au contrôleur BacklogController.js (avec la méthode PUT) et un bouton "supprimer" devra faire appel au même contrôleur mais avec une méthode DELETE. | 3 | T11 | | TODO ||
| T14 | 10 | Dans BacklogController.js ajouter une route PUT et une route DELETE pour la modification et la suppression d'une US. Une fois la suppression ou bien la modification effectuée, rediriger l'utilisateur sur le template backlog. | 2 | | | TODO ||