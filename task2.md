| ID | US ID | Task | Durée | Dépendance | DoD | Etat | Assigné
|:--:|:-----:|------|:-----:|:----------:|-----|:----:|:-------:
|T01|01| Créer et implémenter le fichier Register.ejs contenant le formulaire d'ajout d'un nouvelle utilisateur. Le formulaire devra contenir les champs: adresse email, un peudonyme, un mot de passe et une confirmation du mot de passe. Un bouton "S'inscrire" devra permettre l'inscription de l'utilisateur et un bouton "cancel" pour reinitialiser le formulaire.  La page devra contenir un élément html (div ou alert bootstrap) pour afficher les éventuelles erreurs.| x | y | not yet | TODO | No One
|T02|01| Créer et implémenter le fichier RegisterController.js pour l'enregistrement d'un nouvel utilisateur. L'implémentation devra permettre de gérer une requête GET pour l'affichage du templace register.js et une requête POST pour l'enregistrement du nouvelle utilisateur. Il faudra vérifier que les deux mots de passe reçu sont identiques et que le pseudonyme entré n'est pas utilisé par un autre utilisateur. Si les informations récus dépuis le formulaire sont valide alors rédiriger l'utilisateur sur la page contenant la liste de ses projets sinon l'utilisateur reste sur la même page contenant les erreurs produites.| x | y | not yet | TODO | No One
| T03 | 02 | Créer et implémenter le fichier login.ejs contenant le formulaire de connexion pour un utilisateur. Le formulaire devra permettre la saisie d'une adresse emaill et d'un mot de passe. Un bouton "se connecter" devra permettre la connexion de l'utilisateur et un lien "créer un compte" pour rediriger l'utilisateur vers la page d'inscription (register.ejs). La page de devra contenir un élément html (div ou un alert avec bootstrap) pour afficher les éventuelles erreurs.| | | | |
| T04 | 02 | Créer et implémenter le controlleur login.ejs qui devra proposer une route GET pour afficher la page de connexion (login.ejs) et une route POST pour l'authentification. Si l'authentification a reussie alors rédiriger l'utilisateur sur la page contenant la liste de ses projets. Sinon le rediriger vers la page de connexion en renvoyant les erreurs survenues.| | | | |
| T05 | 03 | Créer et implémenter le temlate createProject.ejs contenant un formulaire pour la création d'un projet. Le formulaire devra contenir un champs pour le titre du projet et un autre pour la description du projet. Un bouton "Créer" dévra rediriger l'utilisateur vers le controlleur "CreateProject.js".| | | | |
| T06 | 03 | Créer et implementer le contrôlleur "CreateProject.js" qui devra proposer une route GET pour afficher le template "createProject.ejs" et une route POST pour ajouter un projet dans la base de données. Il faudra s'assurer que le nom du projet entré n'est pas déjà enregistrer dans la base données. Si l'ajout du projet à reussi alors rédiriger sur l'utilisateur sur la page contenant la liste de ses projets. Sinon le rédiriger sur la page de création de projet (createProject.ejs) avec un message de l'erreur survenue. | | | | |
| T07 | 05 | Créer et implémenter le template "home.ejs" qui va répresenter la page d'accueil de l'utilisateur. Cette page devra proposer un bouton "créer projet", un champs de recherche et une liste des projets aux quelles l'utilisateur participe. La liste des projets devra être affichée sous forme de tableau ayant les colonnes noms, description et propriétaire. un click sur le nom d'un projets devra rédiriger l'utilisateur vers la page d'accueil du projet. Un click sur le bouton "crée projet" devra rédiriger l'utlisateur vers le template de création d'un projet (createProjet.ejs). | | | | |
| T08 | 05 | Créer et implémenter le contrôlleur home.js proposant une route GET (/home/:idUser) pour récupérer la liste des projets de l'utilisateur dont l'id est passé en paramètre dans la requête. L'utilisateur devra être redirigé vers la template home.ejs avec une variable contenant la liste des projets récupérer. Si aucun projet n'a été trouvé alors retourner une liste vide.| | | | |
| T09 | 09 | Créer le template backlog.ejs et y ajouter un bouton "créer U.S". Un click sur ce bouton devra afficher une modale (bootstrap) contenant le formulaire d'ajout d'une US avec les champs description, priorité et difficulté. Un bouton "valider" (sur la modale) devra rediriger vers la page backlogController.js et un bouton cancel (sur la modal) pour reinitialiser le formulaire.| | | | |
| T10 | 09 | Créer le fichier backlogController.js et implémenter une route POST qui permettra d'ajouter un US dans la base de données. Une fois l'ajout effectué rédiriger l'utilisateur vers le template backlog.ejs.| | | | |
| T11 | 11 | Ajouter dans le fichier backlog.ejs un tableau avec les colonnes description, identifiant, importance et difficulté pour afficher les U.S d'un projet de l'utlisateur. | | | | |
| T12 | 11 | Ajouter dans le controlleur BacklogController.js une route GET (/backlog/:idProject) pour récupérer les U.S d'un project. Une fois les U.S récuperer dans la base de données, rediriger l'utlisateur vers le template backlog.js avec une variable contenant la liste des U.S recupérées. Si aucune U.S n'a été trouvée alors renvoyer une liste vide. | | | | |
| T13 | 10 | Dans backlog.ejs faire en sorte qu'un click sur une U.S du backlog fasse apparaître (à droite du backlog) un formulaire préremplie avec les informations du U.S sur le quel l'utilisateur a cliqué.Les informations à mettre dans le formalaire sont la description de la US, son importance (liste déroulante), et sa difficulté. Un bouton "modifier" devra rediriger vers le contrôleur BacklogController.js (avec la méthode PUT) et un bouton "supprimer" devra rédiriger aussi vers le même controlleur mais avec une méthode DELETE.| | | | |
| T14 | 10 | DAns BacklogController.js ajouté une route PUT et une route DELETE pour la modification et la suppression d'une US. Une fois la suppression ou bien la modification effectuée, redirigé l'utilisateur sur la page du backlog (backlog.ejs).| | | | |